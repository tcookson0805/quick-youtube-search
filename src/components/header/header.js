import React from 'react';

import './header.css';

const Header = (props) => {
	return (
		<header>
			<div className="header-text">
				Welcome to Youtube Quick Search
			</div>
		</header>
	)
}

export default Header;